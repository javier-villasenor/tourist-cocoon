info:
  name: Filtrar Huesped
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8080/api/v1/admin/guests/search?q=User
  params:
    - name: q
      value: User
      type: query
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
