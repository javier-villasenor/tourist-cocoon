info:
  name: Ver panel de gestor
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/api/v1/admin/dashboard
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
