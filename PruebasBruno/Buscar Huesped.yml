info:
  name: Buscar Huesped
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8080/api/v1/admin/guests/search
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
